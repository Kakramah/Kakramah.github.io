# تعليمات العمل في بوابة أعمال Kakramah

هذه البوابة تعرض الأعمال العامة فقط. مصدر إدخالاتها `projects.json`، ومصدر تصميم الصفحة `index.template.html`، ويُنتج `build.py` ملف `index.html`.

## القاعدة الدائمة لنشر وإدراج الأعمال
- **شرط الإدراج:** الموقع العام الذي يعتمده خلدون عكرمة بعبارة «جاهز للنشر» يُضاف إلى البوابة بعد اختبار رابطه العام والتأكد من استجابته الحية.
- **الاستثناءات:** يُستثنى من الإدراج في البوابة ما يصفه خلدون بأنه «خارج المكتبة» أو «تجربة».
- **أمر الإيقاف الصارم:** عبارة «لا تنشر» توقف النشر والإدراج فوراً دون أي اجتهاد.

## ضوابط وإجراءات التعديل
- اقرأ README.md وDESIGN.md والملفات التي ستغيّرها قبل التعديل.
- عند إضافة عمل، حدّث `projects.json` وأضف الصورة المصغّرة المطلوبة باسم `images/thumb-<repo>.webp` بمقاس 640×360. لا تخترع عنواناً أو وصفاً أو تاريخاً.
- لا تعدّل `index.html` يدوياً؛ شغّل `python3 build.py` ثم راجع الفرق الناتج.
- لا تعرض اسم مستودع خاص أو رابطه في البوابة إلا إذا طلب المستخدم نشر ذلك العمل صراحة.
- لا تُدرج المواقع العامة غير الموجودة حالياً في الفهرس تلقائياً؛ تحقّق من كونها أعمالاً مقصودة أو تجارب/نسخاً بديلة.
- حافظ على روابط الأعمال المنشورة ومسارات الصور النسبية وإعداد RTL.
- قبل تقديم التغيير، شغّل `python3 build.py`، وتحقق من عدم وجود روابط أو صور مفقودة، واذكر أي رابط لم تستطع اختباره.
- اعمل في فرع وقدّم PR. لا تدمج ولا تنشر تلقائياً إلا ضمن تكليف يجيز ذلك.
- مكتبة خلدون قاعدة عمل Claude. لا تنسخ محتواها إلى هذه البوابة ولا تعدّلها ضمن مهمة صيانة البوابة؛ اتبع تعليمات المشروع هنا مع قاعدة Claude العامة.
