# دليل ربط النطاق الرسمي: kakramah.com

يوضح هذا الدليل الخطوات البسيطة لربط النطاق الرسمي **`kakramah.com`** بموقع البوابة المركزية المستضاف على GitHub Pages، مع تفعيل شهادة الأمان المجانية (HTTPS / SSL).

---

## 1. ما تم إعداده برمجياً في المستودع
تم إنشاء ملف `CNAME` رسمياً داخل مستودع `Kakramah.github.io` ويتضمن:
```text
kakramah.com
```

---

## 2. ضبط سجلات الـ DNS لدى مزود النطاق (Domain Registrar)
عند حجز النطاق (عبر Namecheap أو Cloudflare أو GoDaddy أو أي مزود آخر)، توجه إلى لوحة إدارة النطاق (DNS Management)، وأضف السجلات التالية:

### أ) سجلات A الأربعة الموجهة لخوادم GitHub Pages
أضف أربعة سجلات من نوع **`A`** لربط النطاق الرئيسي (`@` أو اتركه فارغاً حسب مزودك):

| نوع السجل (Type) | الاسم / المضيف (Host) | القيمة / العنوان (IP Address) | مدة الصلاحية (TTL) |
| :--- | :--- | :--- | :--- |
| **A** | `@` | `185.199.108.153` | Automatic أو 3600 |
| **A** | `@` | `185.199.109.153` | Automatic أو 3600 |
| **A** | `@` | `185.199.110.153` | Automatic أو 3600 |
| **A** | `@` | `185.199.111.153` | Automatic أو 3600 |

### ب) سجل CNAME للنطاق الفرعي (www)
أضف سجلاً واحداً من نوع **`CNAME`** لتمكين الزوار الذين يكتبون `www.kakramah.com` من الوصول تلقائياً:

| نوع السجل (Type) | الاسم / المضيف (Host) | القيمة / التوجيه (Target) |
| :--- | :--- | :--- |
| **CNAME** | `www` | `kakramah.github.io.` |

*(ملاحظة: تأكد من كتابة النقطة في نهاية `kakramah.github.io.` إذا كان مزود النطاق يطلب ذلك).*

---

## 3. تفعيل شهادة الأمان المشفرة (Enforce HTTPS)
1. بعد حفظ السجلات، انتظر دقائق معدودة حتى تنتشر السجلات حول العالم.
2. توجه إلى إعدادات المستودع في GitHub:
   `https://github.com/Kakramah/Kakramah.github.io/settings/pages`
3. ستجد خيار **Custom domain** يحتوي بالفعل على `kakramah.com`.
4. قم بتفعيل خيار: **Enforce HTTPS**.
5. ستقوم GitHub آلياً بإصدار شهادة تشفير SSL مجانية من Let's Encrypt لحماية موقعك.

---

## 4. النتيجة النهائية
- سيفتح موقع البوابة المركزية مباشرة عبر: **`https://kakramah.com/`**.
- وسيقوم الرابط القديم `https://kakramah.github.io/` بالتحويل التلقائي والآمن إلى النطاق الجديد دون أن ينكسر أي رابط قديم.
